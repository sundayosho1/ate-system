# Deployment

This directory is reserved for future deployment assets and operational runbooks.

Prompt 1 does not implement production deployment or live trading infrastructure.

Future deployment work must preserve Windows VPS compatibility and must not hard-code local
development paths such as `C:\wamp64\www\ate-system` into runtime logic.
